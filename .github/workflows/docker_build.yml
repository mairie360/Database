name: Docker

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

jobs:
  call-cicd:
    uses: mairie360/CICD/.github/workflows/docker_build.yml@main
    with:
      package_name: database
