name: Docker

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  dev-release:
    uses: mairie360/CICD/.github/workflows/database_cd.yml@main
    with:
      job: "release"
      environment: "dev"
      package_name: database
