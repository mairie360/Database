name: Database CI/CD

on:
  workflow_dispatch:
  push:
    branches:
      - main # main target

permissions:
  contents: read
  packages: write

jobs:
  call-database-cicd:
    uses: mairie360/CICD/.github/workflows/database_cicd.yml@feat/mair-23-add-release-and-tag-creation-for-db
    with:
      package_name: database
