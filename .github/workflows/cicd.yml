name: Database CI/CD

on:
  workflow_dispatch:
  push:

permissions:
  contents: write
  packages: write

jobs:
  Database-cicd:
    uses: mairie360/CICD/.github/workflows/database_cicd.yml@v1.5.1
    with:
      package_name: database
      cicd_version: v1.4.10
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
