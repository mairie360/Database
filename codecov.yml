# Coverage is reported for information only: no minimum is enforced, and the
# codecov/project and codecov/patch checks never fail a PR.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
